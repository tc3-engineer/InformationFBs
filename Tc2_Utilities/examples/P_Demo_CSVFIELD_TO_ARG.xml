<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://www.plcopen.org/xml/tc6_0200">
  <fileHeader companyName="tc3-libraries-kb" productName="TwinCAT" productVersion="3.1" creationDateTime="2026-05-12T10:00:00"/>
  <contentHeader name="Tc2_Utilities.CSVFIELD_TO_ARG Demo" modificationDateTime="2026-05-12T10:00:00">
    <coordinateInfo>
      <fbd><scaling x="1" y="1"/></fbd>
      <ld><scaling x="1" y="1"/></ld>
      <sfc><scaling x="1" y="1"/></sfc>
    </coordinateInfo>
  </contentHeader>
  <types>
    <dataTypes/>
    <pous>
      <pou name="P_Demo_CSVFIELD_TO_ARG" pouType="program">
        <interface>
          <localVars>
            <variable name="sCsvFieldRaw">
              <initialValue><simpleValue value="&quot;42&quot;"/></initialValue>
              <type><string><length>255</length></string></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">来自 CSV 内存缓冲的一段字段（带外层引号）</xhtml></documentation>
            </variable>
            <variable name="nTorqueSetpoint">
              <type><INT/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">解析得到的扭矩设定值（产线下发的工艺参数）</xhtml></documentation>
            </variable>
            <variable name="nBytesWritten">
              <type><UDINT/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">本次写入目标变量的字节数（&gt;0 表示成功）</xhtml></documentation>
            </variable>
            <variable name="bParseRequest">
              <type><BOOL/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">在线置 TRUE 触发一次解析</xhtml></documentation>
            </variable>
          </localVars>
        </interface>
        <body>
          <ST><xhtml xmlns="http://www.w3.org/1999/xhtml">// =============================================================================
// 场景：MES 下发的 CSV 工艺单中，"扭矩设定" 字段是带外层引号的数值
//       （'"42"'）。PLC 需要把该字段解析成 INT 写入控制器变量。
// 价值：单调用同时完成"剥引号"+"INT 转换"两步；如果遇到二进制字段（如
//       时间戳含 0x00 字节）本函数仍可用，CSVFIELD_TO_STRING 会被截断。
// 验证步骤：
//   1. 登录后 nTorqueSetpoint = 0、nBytesWritten = 0。
//   2. 在线写 bParseRequest := TRUE。
//   3. 观察 nTorqueSetpoint 变为 42（剥去外层引号后按 INT 解析）。
//   4. 观察 nBytesWritten = 2（SIZEOF(INT) = 2 字节）。
//   5. 把 sCsvFieldRaw 改成 '42'（无外层引号），保持 bQM := TRUE，
//      会发现首末字符被当成引号丢掉——印证 §5 中 bQM 错配的陷阱。
// =============================================================================

// 单次调用：F_INT 把局部 INT 变量包装成 T_Arg 描述子，函数内通过指针写回
IF bParseRequest THEN
    nBytesWritten := CSVFIELD_TO_ARG(
        pInput  := ADR(sCsvFieldRaw),
        cbInput := LEN(sCsvFieldRaw),
        bQM     := TRUE,
        out     := F_INT(nTorqueSetpoint));
    bParseRequest := FALSE;
END_IF;
</xhtml></ST>
        </body>
      </pou>
    </pous>
  </types>
  <instances><configurations/></instances>
</project>

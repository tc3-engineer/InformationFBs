<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://www.plcopen.org/xml/tc6_0200">
  <fileHeader companyName="tc3-libraries-kb" productName="TwinCAT" productVersion="3.1" creationDateTime="2026-05-12T10:00:00"/>
  <contentHeader name="Tc2_Utilities.DATA_TO_HEXSTR Demo" modificationDateTime="2026-05-12T10:00:00">
    <coordinateInfo>
      <fbd><scaling x="1" y="1"/></fbd>
      <ld><scaling x="1" y="1"/></ld>
      <sfc><scaling x="1" y="1"/></sfc>
    </coordinateInfo>
  </contentHeader>
  <types>
    <dataTypes/>
    <pous>
      <pou name="P_Demo_DATA_TO_HEXSTR" pouType="program">
        <interface>
          <localVars>
            <variable name="dwDeviceSerial">
              <initialValue><simpleValue value="16#BECF1234"/></initialValue>
              <type><DWORD/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">从 EtherCAT SDO 读到的设备序列号</xhtml></documentation>
            </variable>
            <variable name="sHexForLog">
              <type><derived name="T_MaxString"/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">格式化后写入诊断日志的 hex 字符串</xhtml></documentation>
            </variable>
            <variable name="bDumpRequest">
              <type><BOOL/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">在线置 TRUE 触发一次 dump</xhtml></documentation>
            </variable>
          </localVars>
        </interface>
        <body>
          <ST><xhtml xmlns="http://www.w3.org/1999/xhtml">// =============================================================================
// 场景：调试 EtherCAT 从站时，把 CoE SDO 应答原始字节 dump 到诊断日志，
//       便于和厂商规范文档比对（例如查看序列号是不是按小端存）。
// 价值：单调用得到空格分隔的 hex 串，直接和 Wireshark / 厂商 datasheet 对照；
//       手写格式化循环要 10+ 行且容易漏 0 填充。
// 验证步骤：
//   1. 登录后 dwDeviceSerial = 16#BECF1234（在线 monitor 显示十进制 3201528372）。
//   2. 在线写 bDumpRequest := TRUE。
//   3. 观察 sHexForLog 变为 '34 12 CF BE'（注意是小端！最低字节先）。
//   4. 把 dwDeviceSerial 改成 16#00000000，再次触发，sHexForLog = '00 00 00 00'。
//   5. 验证截断：如要观察 §3 中的 '.' 截断行为，可将 cbData 改成 90（&gt;85），
//      返回串末尾会出现 '.'；本例保持 SIZEOF 不演示截断。
// =============================================================================

IF bDumpRequest THEN
    // 单次调用：bLoCase := FALSE 保持大写 hex，方便和规范文档比对
    sHexForLog := DATA_TO_HEXSTR(
        pData   := ADR(dwDeviceSerial),
        cbData  := SIZEOF(dwDeviceSerial),
        bLoCase := FALSE);
    bDumpRequest := FALSE;
END_IF;
</xhtml></ST>
        </body>
      </pou>
    </pous>
  </types>
  <instances><configurations/></instances>
</project>

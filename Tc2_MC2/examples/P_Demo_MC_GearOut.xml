<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://www.plcopen.org/xml/tc6_0200">
  <fileHeader companyName="tc3-libraries-kb" productName="TwinCAT" productVersion="3.1" creationDateTime="2026-05-21T00:00:00"/>
  <contentHeader name="Tc2_MC2.MC_GearOut Demo" modificationDateTime="2026-05-21T00:00:00">
    <coordinateInfo>
      <fbd><scaling x="1" y="1"/></fbd>
      <ld><scaling x="1" y="1"/></ld>
      <sfc><scaling x="1" y="1"/></sfc>
    </coordinateInfo>
  </contentHeader>
  <types>
    <dataTypes/>
    <pous>
      <pou name="P_Demo_MC_GearOut" pouType="program">
        <interface>
          <localVars>
            <variable name="fbDecouple"><type><derived name="MC_GearOut"/></type></variable>
            <variable name="fbHaltSlave"><type><derived name="MC_Halt"/></type></variable>
            <variable name="axisSlave"><type><derived name="AXIS_REF"/></type></variable>
            <variable name="rtDecoupleTrig"><type><derived name="R_TRIG"/></type></variable>
            <variable name="bRequestDecouple"><initialValue><simpleValue value="FALSE"/></initialValue><type><BOOL/></type></variable>
            <variable name="bDecoupleDone"><type><BOOL/></type></variable>
            <variable name="bSlaveStopDone"><type><BOOL/></type></variable>
            <variable name="nErrorID"><type><UDINT/></type></variable>
          </localVars>
        </interface>
        <body>
          <ST><xhtml xmlns="http://www.w3.org/1999/xhtml">// =============================================================================
// 场景：电子齿轮耦合工艺段完成 — 先解耦从轴让它脱离主轴跟随，然后立刻
//       发 MC_Halt 把从轴平稳停下来。直接发 MC_GearOut 不接停车从轴会
//       无限走（PDF DANGER 提醒）。
// 价值：标准的"解耦 + 停车"两步法；避免冲软限位 / 撞机械限位事故。
// 验证：先建立耦合（外部 MC_GearIn 已经调过，demo 假设 axisSlave 在跟主轴跑）；
//       bRequestDecouple := TRUE → bDecoupleDone 一闪 TRUE；
//       同周期触发 fbHaltSlave 减速；ActVelo 渐降到 0；
//       bSlaveStopDone 短暂 TRUE。
// =============================================================================

rtDecoupleTrig(CLK := bRequestDecouple);

fbDecouple(
    Execute := rtDecoupleTrig.Q,
    Slave   := axisSlave,
    Done    =&gt; bDecoupleDone,
    ErrorID =&gt; nErrorID
);

// 解耦完成立刻接停车 — 这是 PDF 反复强调的"务必做"步骤
fbHaltSlave(
    Execute      := bDecoupleDone,
    Deceleration := 2000.0,
    Jerk         := 20000.0,
    Axis         := axisSlave,
    Done         =&gt; bSlaveStopDone
);
</xhtml></ST>
        </body>
      </pou>
    </pous>
  </types>
  <instances><configurations/></instances>
</project>

<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://www.plcopen.org/xml/tc6_0200">
  <fileHeader companyName="tc3-libraries-kb" productName="TwinCAT" productVersion="3.1" creationDateTime="2026-05-12T10:00:00"/>
  <contentHeader name="Tc2_Utilities.CSVFIELD_TO_STRING Demo" modificationDateTime="2026-05-12T10:00:00">
    <coordinateInfo>
      <fbd><scaling x="1" y="1"/></fbd>
      <ld><scaling x="1" y="1"/></ld>
      <sfc><scaling x="1" y="1"/></sfc>
    </coordinateInfo>
  </contentHeader>
  <types>
    <dataTypes/>
    <pous>
      <pou name="P_Demo_CSVFIELD_TO_STRING" pouType="program">
        <interface>
          <localVars>
            <variable name="sCsvProductName">
              <initialValue><simpleValue value="&quot;60&quot;&quot;TV&quot;"/></initialValue>
              <type><string><length>255</length></string></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">从 CSV 配方表读出的"产品名称"字段（含转义双引号）</xhtml></documentation>
            </variable>
            <variable name="sProductNameDecoded">
              <type><string><length>255</length></string></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">去转义后可直接写入产线 HMI 的实际名称</xhtml></documentation>
            </variable>
            <variable name="bDecodeRequest">
              <type><BOOL/></type>
              <documentation><xhtml xmlns="http://www.w3.org/1999/xhtml">在线置 TRUE 触发一次解析</xhtml></documentation>
            </variable>
          </localVars>
        </interface>
        <body>
          <ST><xhtml xmlns="http://www.w3.org/1999/xhtml">// =============================================================================
// 场景：MES CSV 配方表的"产品名称"列含双引号字符（如 60"TV），写入 CSV
//       时按规范转义为 '"60""TV"'。PLC 读出这段后需要还原为 '60"TV' 显示。
// 价值：替代手写 REPLACE('""', '"') 链；自动剥外层引号 + 还原内嵌引号，
//       与 STRING_TO_CSVFIELD 写入时严格对称（writer/reader 一致）。
// 验证步骤：
//   1. 登录后 sProductNameDecoded = ''。
//   2. 在线写 bDecodeRequest := TRUE。
//   3. 观察 sProductNameDecoded 变为 '60"TV'（外引号去掉、'""' 变 '"'）。
//   4. 把 sCsvProductName 改成 '60""TV'（无外引号），bQM 保持 TRUE：
//      首字符 '6' 和尾字符 'V' 被当成外层引号丢掉，结果错——印证 bQM 错配陷阱。
// =============================================================================

IF bDecodeRequest THEN
    // 单次调用：bQM := TRUE 因为 writer 端用的就是 STRING_TO_CSVFIELD(s, TRUE)
    sProductNameDecoded := CSVFIELD_TO_STRING(in := sCsvProductName, bQM := TRUE);
    bDecodeRequest := FALSE;
END_IF;
</xhtml></ST>
        </body>
      </pou>
    </pous>
  </types>
  <instances><configurations/></instances>
</project>
